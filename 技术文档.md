# AI 任务拆解系统 — 完整技术文档

> **版本**：v1.0  
> **最后更新**：2026-04-02  
> **项目仓库**：`ai-task-decomposer`

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [技术栈](#3-技术栈)
4. [项目目录结构](#4-项目目录结构)
5. [环境变量与配置](#5-环境变量与配置)
6. [数据库设计](#6-数据库设计)
7. [后端 API 接口文档](#7-后端-api-接口文档)
8. [前端技术细节](#8-前端技术细节)
9. [核心业务流程](#9-核心业务流程)
10. [安全机制](#10-安全机制)
11. [部署指南](#11-部署指南)

---

## 1. 项目概述

### 1.1 产品定位

AI 任务拆解系统是一款基于大语言模型（LLM）的智能任务管理工具。用户输入一个宏观任务目标，系统自动将其拆解为可执行的分步骤流程图，并支持对每个节点进行深入对话，获取详细的执行指导。

### 1.2 核心功能

| 功能模块 | 描述 |
|---------|------|
| **任务分解** | 用户输入任务描述（支持文本+图片），AI 自动生成结构化的任务树 |
| **导入已有导图** | 支持上传思维导图图片或 Markdown 文件，AI 提取层级结构并转换 |
| **节点对话** | 对任意任务节点发起提问，获取 AI 生成的执行方案（流式响应） |
| **全局问答** | 针对整棵任务树进行全局性提问（关键路径、风险分析等） |
| **用户认证** | 账号密码登录（自动注册）、WebAuthn 通行证免密登录 |
| **云端历史** | 任务树的保存、恢复与云端持久化 |
| **进度管理** | 节点对话进度追踪、Markdown 渲染、图片导出 |
| **编辑模式** | 拖拽调整节点层级、添加/删除节点、撤销/重做 |
| **开发者模式** | 支持自定义 AI API（Key、Base URL、Model、请求体） |
| **密码找回** | 邮箱绑定 + 邮件验证码重置密码 |

### 1.3 设计原则

- **前后端分离**：前端纯静态 HTML/CSS/JS，后端 Node.js REST API
- **流式体验**：所有 AI 交互均采用 SSE（Server-Sent Events）流式传输
- **Material Design 3**：前端遵循 Google MD3 设计规范
- **渐进增强**：未登录用户可使用基础功能（访客模式），登录后解锁云端存储

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                      客户端浏览器                         │
│  ┌─────────────────┐  ┌─────────────────┐               │
│  │   PC 端          │  │   移动端         │               │
│  │  index.html      │  │ mobile/index.html│               │
│  │  login.html      │  │ mobile/login.html│               │
│  └────────┬─────────┘  └────────┬─────────┘               │
│           │                      │                        │
│           └──────────┬───────────┘                        │
│                      │ HTTPS                              │
└──────────────────────┼────────────────────────────────────┘
                       │
          ┌────────────▼────────────┐
          │   Nginx 反向代理         │
          │   hzyweb.xyz            │
          │   m.hzyweb.xyz          │
          └────────────┬────────────┘
                       │
          ┌────────────▼────────────┐
          │   Express 后端服务器     │
          │   (server.js)           │
          │   Port: 3000            │
          ├─────────────────────────┤
          │  REST API + SSE         │
          │  JWT 认证中间件         │
          │  WebAuthn 认证          │
          └────┬───────┬───────┬───┘
               │       │       │
      ┌────────▼──┐ ┌──▼────┐ ┌▼──────────┐
      │  SQLite   │ │ 智谱AI│ │ 腾讯邮箱  │
      │  数据库   │ │ GLM   │ │ SMTP      │
      │ai_tasks.db│ │ 4.6V  │ │ 465/SSL   │
      └───────────┘ └───────┘ └───────────┘
```

### 2.2 数据流

```
用户输入 → [前端处理] → POST /api/decompose → [后端]
                                              ├─ 构建 Prompt
                                              ├─ 调用智谱 AI API（SSE）
                                              └─ 流式透传 → 前端逐步渲染
```

---

## 3. 技术栈

### 3.1 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | - | 运行时环境 |
| Express | 5.2.1 | Web 框架（v5） |
| better-sqlite3 | 12.8.0 | SQLite 数据库驱动（同步 API） |
| axios | 1.13.6 | HTTP 请求客户端（调用智谱 AI） |
| jsonwebtoken | 9.0.3 | JWT 令牌签发与验证 |
| bcrypt | 6.0.0 | 密码哈希加密 |
| @simplewebauthn/server | 13.3.0 | WebAuthn 服务端实现 |
| nodemailer | 8.0.4 | 邮件发送（密码找回） |
| cors | 2.8.6 | 跨域资源共享 |
| dotenv | 17.3.1 | 环境变量管理 |

### 3.2 前端

| 技术 | 用途 |
|------|------|
| 原生 HTML5/CSS3/ES6+ | 无框架，纯手写 |
| Material Icons Round | Google 图标库 |
| SimpleWebAuthn Browser | WebAuthn 浏览器端实现 |
| marked.js | Markdown 渲染（CDN） |
| html2canvas | 任务树导出为图片（CDN） |
| Source Han Serif SC | 思源宋体，加载页字体 |
| Roboto | 主字体族（Google Fonts） |

### 3.3 第三方服务

| 服务 | 用途 |
|------|------|
| 智谱 AI (bigmodel.cn) | GLM-4.6V 大语言模型（任务分解 + 节点对话） |
| Hitokoto API | 加载页随机一言 |
| 腾讯 QQ/企业邮箱 SMTP | 密码重置验证码邮件 |

---

## 4. 项目目录结构

```
ai-task-decomposer/
├── backend/
│   ├── server.js          # 后端主文件（所有路由、中间件、数据库初始化）
│   ├── package.json       # 后端依赖配置
│   ├── package-lock.json  # 依赖锁定文件
│   └── ai_tasks.db        # SQLite 数据库文件（运行时生成）
├── front/
│   ├── index.html         # PC 端主页面（任务分解 + 编辑 + 对话）
│   ├── login.html         # PC 端登录/注册页面
│   ├── mobile/
│   │   ├── index.html     # 移动端主页面
│   │   └── login.html     # 移动端登录页面
│   ├── favicon.ico        # 网站图标
│   ├── favicon-32x32.ico  # 32px 网站图标
│   └── robots.txt         # 搜索引擎爬虫规则
├── md-im-app/             # 相关子项目（独立）
├── 用户使用说明.md         # 用户操作手册
├── 技术文档.md            # 本文档
├── README.md              # 项目说明
└── LICENSE                # 开源许可证
```

> **说明**：后端采用单文件架构 (`server.js`)，所有路由、中间件、数据库建表均在一个文件中完成，适合中小型项目快速迭代。

---

## 5. 环境变量与配置

### 5.1 `.env` 文件配置

```env
# JWT 签名密钥（必须修改为高强度随机字符串）
JWT_SECRET=your-super-secret-key

# 智谱 AI API Key
ZHIPU_API_KEY=your_zhipu_api_key_here

# WebAuthn 配置
RP_ID=hzyweb.xyz
EXPECTED_ORIGIN=https://hzyweb.xyz,https://m.hzyweb.xyz

# 邮件服务配置（QQ 邮箱 / 腾讯企业邮箱）
MAIL_USER=your_email@qq.com
MAIL_PASS=your_smtp_auth_code
MAIL_FROM_NAME=AI任务拆解系统

# 服务端口
PORT=3000
```

### 5.2 环境变量详细说明

| 变量名 | 必需 | 默认值 | 说明 |
|--------|------|--------|------|
| `JWT_SECRET` | 是 | `your-super-secret-key` | JWT 签名密钥，**生产环境务必修改** |
| `ZHIPU_API_KEY` | 是 | 无 | 智谱 AI 开放平台 API Key |
| `RP_ID` | 否 | `hzyweb.xyz` | WebAuthn Relying Party ID |
| `EXPECTED_ORIGIN` | 否 | `https://hzyweb.xyz,https://m.hzyweb.xyz` | 允许的来源域名（逗号分隔） |
| `MAIL_USER` | 否 | 空 | 发件邮箱地址（不配置则开发模式直接返回验证码） |
| `MAIL_PASS` | 否 | 空 | 邮箱 SMTP 授权码 |
| `MAIL_FROM_NAME` | 否 | `AI任务拆解系统` | 邮件显示的发件人名称 |
| `PORT` | 否 | `3000` | 后端服务监听端口 |

### 5.3 邮箱类型自动识别

系统根据 `MAIL_USER` 的域名自动选择 SMTP 服务器：

| 邮箱域名 | SMTP 服务器 | 端口 |
|---------|------------|------|
| `@qq.com` / `@foxmail.com` | `smtp.qq.com` | 465 (SSL) |
| 其他域名（企业邮箱） | `smtp.exmail.qq.com` | 465 (SSL) |

---

## 6. 数据库设计

### 6.1 数据库概览

使用 **SQLite**（通过 `better-sqlite3`），数据库名为 `ai_tasks.db`，运行时自动创建。所有时间字段使用 `datetime('now', '+8 hours')` 自动填充北京时间。

### 6.2 ER 关系图

```
┌──────────┐       ┌─────────────────────┐
│  users   │       │ webauthn_credentials │
├──────────┤       ├─────────────────────┤
│ id (PK)  │──┐    │ id (PK)             │
│ username │  └───→│ user_id (FK)        │
│ password │       │ credential_id (UQ)  │
│ email    │       │ public_key          │
└──────────┘       │ counter             │
      │            │ device_name         │
      │            │ created_at          │
      │            └─────────────────────┘
      │
      ├──────┬──────────────┬──────────────────┐
      │      │              │                  │
┌─────▼───┐ ┌▼────────────┐ ┌▼─────────────┐ ┌▼──────────────────┐
│chat_    │ │ user_trees  │ │ node_states  │ │ global_chat_      │
│history  │ │             │ │              │ │ history           │
├─────────┤ ├─────────────┤ ├──────────────┤ ├──────────────────┤
│ id (PK) │ │ id (PK)     │ │ node_id (PK) │ │ id (PK)          │
│ user_id │ │ user_id     │ │ title        │ │ tree_id          │
│ node_id │ │ title       │ │ desc         │ │ role             │
│ role    │ │ tree_data   │ │ summary      │ │ content          │
│ content │ │ created_at  │ │ recent_chat  │ │ created_at       │
│created_ │ └─────────────┘ │ chat_count   │ └──────────────────┘
│ at      │                 └──────────────┘
└─────────┘
```

### 6.3 表结构详细

#### users — 用户表

```sql
CREATE TABLE IF NOT EXISTS users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE,          -- 用户名（登录凭据）
    password TEXT,                 -- bcrypt 哈希加密后的密码
    email TEXT                     -- 绑定邮箱（ALTER TABLE 迁移添加）
);
```

> **迁移说明**：`email` 字段通过 `ALTER TABLE` 在服务启动时动态添加，兼容旧数据库。

#### node_states — 节点短记忆状态表（上下文压缩）

```sql
CREATE TABLE IF NOT EXISTS node_states (
    node_id TEXT PRIMARY KEY,      -- 节点唯一标识
    title TEXT,                    -- 节点标题
    desc TEXT,                     -- 节点描述
    summary TEXT DEFAULT '',       -- AI 生成的历史摘要
    recent_chat TEXT DEFAULT '[]', -- 最近对话记录（JSON 数组）
    chat_count INTEGER DEFAULT 0   -- 累计对话轮次（达到 4 轮触发压缩）
);
```

#### chat_history — 全量聊天历史表

```sql
CREATE TABLE IF NOT EXISTS chat_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,               -- 关联用户 ID
    node_id TEXT,                  -- 关联节点 ID
    role TEXT,                     -- 角色：user / assistant
    content TEXT,                  -- 消息内容
    created_at DATETIME DEFAULT (datetime('now', '+8 hours'))
);
```

#### user_trees — 整树快照表

```sql
CREATE TABLE IF NOT EXISTS user_trees (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,               -- 关联用户 ID
    title TEXT,                    -- 树标题（截取任务描述前 15 字）
    tree_data TEXT,                -- 完整树数据（JSON）
    created_at DATETIME DEFAULT (datetime('now', '+8 hours'))
);
```

> **tree_data JSON 结构**：
> ```json
> {
>   "mainNodes": [
>     { "id": "1", "parentId": null, "title": "步骤名称", "desc": "描述" },
>     { "id": "2", "parentId": "1", "title": "子步骤", "desc": "子描述" }
>   ],
>   "progressNodes": {
>     "1": [{ "content": "AI 回复的 Markdown 内容" }]
>   }
> }
> ```

#### webauthn_credentials — WebAuthn 凭证表

```sql
CREATE TABLE IF NOT EXISTS webauthn_credentials (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,               -- 关联用户 ID
    credential_id TEXT UNIQUE,     -- WebAuthn 凭证 ID（Base64）
    public_key TEXT,               -- 公钥（Base64）
    counter INTEGER,               -- 签名计数器（防重放攻击）
    device_name TEXT DEFAULT '未命名设备',  -- 设备名称
    created_at DATETIME DEFAULT (datetime('now', '+8 hours'))
);
```

#### global_chat_history — 全局对话历史表

```sql
CREATE TABLE IF NOT EXISTS global_chat_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tree_id INTEGER,               -- 关联树 ID
    role TEXT,                     -- 角色：user / assistant
    content TEXT,                  -- 消息内容
    created_at DATETIME DEFAULT (datetime('now', '+8 hours'))
);
```

---

## 7. 后端 API 接口文档

**API 基址**：`https://api.hzyweb.xyz`（通过 Nginx 反向代理到后端 3000 端口）

### 7.1 认证中间件

| 中间件名称 | 说明 |
|-----------|------|
| `optionalAuthMiddleware` | 宽松鉴权：有 Token 则解析 userId，无则设为 null（访客放行） |
| `strictAuthMiddleware` | 严格鉴权：必须携带有效 Token，否则返回 401/403 |

### 7.2 用户认证接口

#### POST `/api/login` — 登录（自动注册）

> **无需鉴权**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `username` | string | 是 | 用户名 |
| `password` | string | 是 | 密码 |

**逻辑**：
1. 查询用户是否存在
2. 不存在 → 自动注册（bcrypt 哈希密码后插入）
3. 已存在 → 比对 bcrypt 哈希
4. 成功 → 签发 JWT（有效期 7 天）

**响应**：
```json
{ "token": "eyJhbG...", "message": "认证成功" }
```

**错误码**：`401` 密码错误 | `500` 服务器错误

---

#### POST `/api/user/email` — 绑定/更新邮箱

> **严格鉴权**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `email` | string | 是 | 邮箱地址 |

**错误码**：`400` 格式错误 | `409` 邮箱已被绑定 | `500` 更新失败

---

#### POST `/api/password/send-reset-code` — 发送重置验证码

> **无需鉴权**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `username` | string | 是 | 用户名 |

**逻辑**：
1. 验证用户名存在且已绑定邮箱
2. 频率限制：60 秒内仅可发送一次
3. 生成 6 位随机验证码（5 分钟有效，内存存储）
4. 发送 HTML 格式邮件（未配置邮箱时开发模式直接返回验证码）

**响应**：
```json
{ "success": true, "message": "验证码已发送至 ab***@qq.com", "maskedEmail": "ab***@qq.com" }
```

**开发模式响应**（未配置邮箱）：
```json
{ "success": true, "message": "验证码已生成（仅供调试）", "devCode": "123456" }
```

---

#### POST `/api/password/reset` — 验证重置码并改密

> **无需鉴权**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `username` | string | 是 | 用户名 |
| `code` | string | 是 | 6 位验证码 |
| `newPassword` | string | 是 | 新密码（至少 6 位） |

**错误码**：`400` 参数不完整/验证码错误/已过期 | `404` 用户不存在 | `500` 重置失败

---

### 7.3 WebAuthn 接口

#### GET `/api/auth/register/options` — 生成注册选项

> **严格鉴权**

**响应**：WebAuthn `PublicKeyCredentialCreationOptions` JSON

---

#### POST `/api/auth/register/verify` — 验证并绑定设备

> **严格鉴权**

**请求体**：WebAuthn 注册响应 + `deviceName`

**响应**：
```json
{ "success": true }
```

---

#### POST `/api/auth/login/options` — 生成登录选项

> **无需鉴权**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `username` | string | 是 | 用户名 |

**响应**：
```json
{ "options": { ... }, "userId": 123 }
```

---

#### POST `/api/auth/login/verify` — 验证登录签名

> **无需鉴权**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `assertion` | object | 是 | WebAuthn 断言响应 |
| `userId` | number | 是 | 用户 ID |

**响应**：
```json
{ "success": true, "token": "eyJhbG...", "message": "通行证认证成功" }
```

---

#### GET `/api/auth/credentials` — 获取通行证列表

> **严格鉴权**

**响应**：
```json
[
  { "id": 1, "device_name": "Windows 电脑", "created_at": "2026-03-20 10:00:00" }
]
```

---

#### DELETE `/api/auth/credentials/:id` — 删除通行证

> **严格鉴权** | **防越权**：必须同时匹配 `id` 和当前 `user_id`

---

#### PUT `/api/auth/credentials/:id` — 重命名通行证

> **严格鉴权**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `deviceName` | string | 是 | 新的设备名称 |

---

### 7.4 任务分解接口

#### POST `/api/decompose` — AI 任务分解（SSE 流式）

> **无需鉴权** | **支持开发者模式**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `taskText` | string | 是 | 任务描述文本 |
| `images` | string[] | 否 | Base64 编码的图片数组 |

**System Prompt**：
```
你是一名专业的任务拆解专家，请按照逻辑链将任务拆分为细小步骤。
你必须且只能严格按照 JSON 数组格式输出：
[{"id": "唯一数字或字符串", "parentId": "父节点 id", "title": "步骤名称", "desc": "具体操作内容"}]
```

**响应**：`text/event-stream` — 直接将智谱 AI 的 SSE 流透传给前端

**开发者模式**：通过 HTTP Header 覆盖默认配置：
- `X-Dev-Key`：自定义 API Key
- `X-Dev-Base`：自定义 Base URL
- `X-Dev-Model`：自定义模型名
- `X-Dev-Body`：自定义请求体（JSON）

---

#### POST `/api/import` — 导入已有导图（SSE）

> **无需鉴权** | **非流式**：后端一次性获取结果后模拟 SSE 格式返回

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `taskText` | string | 是 | 文本内容（Markdown） |
| `images` | string[] | 否 | 思维导图图片（Base64） |

---

### 7.5 节点对话接口

#### POST `/api/ask` — 节点对话（SSE 流式 + 上下文压缩）

> **宽松鉴权**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `nodeId` | string | 是 | 节点 ID |
| `title` | string | 是 | 节点标题 |
| `desc` | string | 是 | 节点描述 |
| `userQuestion` | string | 否 | 用户问题（留空默认索要执行方案） |

**上下文压缩机制**：
1. 每次对话写入 `node_states` 表的 `recent_chat`
2. 累计 4 轮对话后触发 AI 摘要压缩
3. 压缩结果写入 `summary` 字段，`recent_chat` 清空
4. 后续对话携带 `summary` 作为历史上下文

**数据双写**（已登录用户）：
- 全量聊天记录 → `chat_history` 表（云端持久化备份）
- 短记忆 → `node_states` 表（上下文压缩用）

---

#### GET `/api/history` — 获取节点聊天历史

> **严格鉴权**

| 查询参数 | 类型 | 必需 | 说明 |
|---------|------|------|------|
| `nodeId` | string | 是 | 节点 ID |

---

### 7.6 整树状态接口

#### GET `/api/trees` — 获取历史树列表

> **严格鉴权**

**响应**：
```json
[
  { "id": 1, "title": "营销方案...", "created_at": "2026-03-20 10:00:00" }
]
```

---

#### GET `/api/trees/:id` — 获取单棵树完整数据

> **严格鉴权**

**响应**：`tree_data` 解析后的 JSON 对象

---

#### POST `/api/trees` — 保存新树

> **严格鉴权**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `title` | string | 是 | 树标题 |
| `tree_data` | object | 是 | 树数据对象 |

**响应**：
```json
{ "success": true, "id": 42 }
```

---

#### PUT `/api/trees/:id` — 更新树数据

> **严格鉴权**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `tree_data` | object | 是 | 更新后的树数据 |

---

### 7.7 全局树问答接口

#### POST `/api/chat-tree` — 全局问答（SSE 流式）

> **无需鉴权**

| 参数 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `treeId` | number | 否 | 树 ID（有则保存对话历史） |
| `treeData` | object | 是 | 完整树数据 `{ mainNodes, progressNodes }` |
| `question` | string | 是 | 用户问题 |
| `history` | array | 否 | 对话历史 |

---

#### GET `/api/chat-tree/history/:treeId` — 获取全局对话历史

> **无需鉴权**

---

## 8. 前端技术细节

### 8.1 页面结构

#### PC 端 `index.html`（~2871 行，单文件 SPA）

**HTML 结构层级**：

```
<body>
├── <header class="top-bar">              # 顶部导航栏
│   ├── .logo (图标 + 标题)
│   └── .top-bar-actions
│       ├── #modeSwitcher (查看/修改 切换)
│       ├── #authBtn (登录/个人中心)
│       ├── #exportBtn (导出图片)
│       └── #clearAllBtn (清空所有)
├── <div class="main-content">            # 主内容区
│   ├── #usageGuide (使用说明浮窗)
│   └── #treeContainer > #treeRoot        # 任务树容器
├── #configToggleBtn (底部 FAB 按钮)
├── #configPanel (任务配置面板)
│   ├── 工作模式切换 (分解/导入)
│   ├── 任务描述 textarea
│   ├── 文件上传 input
│   ├── 主题颜色选择器
│   ├── 云端历史记录列表
│   └── 操作按钮 (重置/开始分解)
├── #detailPanel (进度详情面板)
├── #accountPanel (个人中心面板)
│   ├── 通行证管理
│   ├── 邮箱绑定
│   ├── 开发者模式
│   └── 退出登录
├── #globalChatPanel (全局问答面板)
├── #mdDialogOverlay (通用 MD3 对话框)
└── .undo-redo-container (撤销/重做)
```

#### PC 端 `login.html`

**功能**：
- 用户名 + 密码登录
- WebAuthn 通行证一键登录
- 密码找回（三步弹窗：输入用户名 → 输入验证码 → 设置新密码）
- 使用说明链接跳转

### 8.2 CSS 设计体系

#### Material Design 3 设计令牌

```css
:root {
    --md-sys-color-primary: #6750A4;           /* 主色（紫色） */
    --md-sys-color-primary-container: #EADDFF; /* 主色容器 */
    --md-sys-color-on-primary-container: #21005D;
    --md-sys-color-surface: #FEF7FF;           /* 表面色 */
    --md-sys-color-on-surface: #1D1B20;        /* 表面色上的文字 */
    --md-sys-color-surface-variant: #E7E0EC;
    --md-sys-color-on-surface-variant: #49454F;
    --md-sys-color-outline: #79747E;           /* 边框色 */
    --md-sys-color-outline-variant: #CAC4D0;
    --md-sys-elevation-1 ~ 4;                  /* 4 级阴影 */
    --md-sys-shape-corner-medium: 12px;
    --md-sys-shape-corner-large: 16px;
    --md-sys-shape-corner-extra-large: 28px;
    --md-sys-shape-corner-full: 9999px;        /* 胶囊形 */
    --md-sys-motion-duration-medium: 300ms;
}
```

#### 主题颜色切换

用户可通过颜色选择器动态修改 `--md-sys-color-primary`，提供 5 种预设色：
- 紫色 `#6750A4`（默认）、蓝色 `#1976D2`、绿色 `#388E3C`、红色 `#D32F2F`、橙色 `#F57C00`

#### 响应式断点

| 断点 | 目标设备 | 关键调整 |
|------|---------|---------|
| `≤ 1024px` | 平板 | 使用指南宽度缩减、树容器内边距减小 |
| `≤ 768px` | 手机 | 配置面板全屏高度、节点气泡缩小、进度面板全屏 |
| `≤ 480px` | 小屏手机 | 标题截断、按钮尺寸缩小、节点容器纵向排列 |

### 8.3 JavaScript 核心架构

#### 8.3.1 全局状态管理

```javascript
const state = {
    mainNodes: [],           // 主节点数组（任务树核心数据）
    progressNodes: {},       // 进度数据 { nodeId: [{ content: "..." }] }
    isProcessing: false,     // 防止重复请求
    configPanelVisible: true,// 配置面板可见状态
    scale: 1,                // 树缩放比例
    currentTreeId: null,     // 当前云端树 ID
    editMode: false,         // 编辑模式开关
    nodeAsking: null,        // 当前提问中的节点 ID（防并发）
    globalHistory: [],       // 全局对话历史
    undoStack: [],           // 撤销栈（最多 30 条）
    redoStack: []            // 重做栈
};
```

#### 8.3.2 核心函数清单

| 函数名 | 功能 |
|--------|------|
| `fetchSSE(url, body, onUpdate)` | 通用 SSE 流式请求（自动携带 JWT + 开发者 Header） |
| `extractFilesContent()` | 提取上传文件内容（图片转 Base64、文本追加） |
| `handleDecompose()` | 任务分解主流程 |
| `renderTree()` | 根据 state 渲染完整任务树 DOM |
| `buildHtml(node)` | 递归构建单个节点及子树的 HTML |
| `askNode(nodeId)` | 节点对话（防并发锁） |
| `handleGlobalChat()` | 全局问答 |
| `pushState()` / `undo()` / `redo()` | 撤销/重做 |
| `addNode(parentId)` / `confirmDelete(nodeId)` | 节点增删 |
| `handleDragStart/Over/Drop()` | 拖拽调整父子关系 |
| `saveCurrentTree()` | 自动保存到云端 |
| `loadHistoryList()` / `restoreTree(id)` | 历史树加载还原 |
| `exportAsImage()` | html2canvas 导出 PNG |
| `showMdDialog(options)` | 通用 Promise 化对话框 |
| `updateAuthState()` | 认证状态同步 |
| `getDevHeaders()` | 读取开发者模式配置 |
| `loadPasskeys()` / `deletePasskey()` / `renamePasskey()` | 通行证管理 |
| `loadBindEmail()` | 加载已绑定邮箱 |

#### 8.3.3 SSE 流式处理

```javascript
async function fetchSSE(url, bodyData, onUpdate) {
    const token = localStorage.getItem('jwt_token');
    const headers = {
        'Content-Type': 'application/json',
        ...getDevHeaders()  // 开发者模式 Header
    };
    if (token) headers['Authorization'] = `Bearer ${token}`;

    const response = await fetch(url, {
        method: 'POST',
        headers: headers,
        body: JSON.stringify(bodyData)
    });

    const reader = response.body.getReader();
    const decoder = new TextDecoder();
    let fullText = '', buffer = '';

    while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        buffer += decoder.decode(value, { stream: true });
        let lines = buffer.split('\n');
        buffer = lines.pop();
        for (let line of lines) {
            line = line.trim();
            if (line.startsWith('data: ') && line !== 'data: [DONE]') {
                const content = JSON.parse(line.slice(6))
                    .choices?.[0]?.delta?.content || "";
                if (content) {
                    fullText += content;
                    if (onUpdate) onUpdate(fullText, false);
                }
            }
        }
    }
    if (onUpdate) onUpdate(fullText, true);
    return fullText;
}
```

#### 8.3.4 任务树渲染方式

采用 **纯 DOM 树形结构**（非 Canvas/SVG），利用 CSS 伪元素绘制连接线：

```html
<ul>                          <!-- 子节点容器 -->
  <li>                        <!-- 每个节点 -->
    <div class="node-container">
      <div class="node-bubble">  <!-- 节点气泡 -->
        <div class="node-header">
          <div class="node-number">1</div>
          <div class="node-title">标题</div>
        </div>
        <div class="node-desc">描述</div>
        <div class="node-action">
          <button>提问</button>
        </div>
      </div>
      <div class="progress-container">  <!-- 进度节点 -->
        <div class="progress-node">...</div>
      </div>
    </div>
    <ul>...</ul>               <!-- 递归子节点 -->
  </li>
</ul>
```

CSS 伪元素连接线：
- `li::before` / `li::after`：绘制水平连接线和垂直线
- `ul ul::before`：绘制从父节点到子节点层的垂直线
- 首尾节点的连线通过 `:first-child` / `:last-child` 特殊处理圆角

#### 8.3.5 撤销/重做机制

```javascript
function pushState() {
    const snapshot = {
        mainNodes: JSON.parse(JSON.stringify(state.mainNodes)),
        progressNodes: JSON.parse(JSON.stringify(state.progressNodes))
    };
    state.undoStack.push(snapshot);
    if (state.undoStack.length > 30) state.undoStack.shift();
    state.redoStack = [];
}
```

- 快捷键：`Ctrl+Z` 撤销、`Ctrl+Y` / `Ctrl+Shift+Z` 重做
- 最大保存 30 条历史记录
- 执行新操作时清空重做栈

#### 8.3.6 页面间数据传递

- **JWT Token**：通过 `localStorage` 存储，同时尝试从 Cookie 同步（跨子域场景）
- **跨域 Cookie**：`setCrossDomainCookie()` 自动设置 `.hzyweb.xyz` 级别的域名

### 8.4 拖拽与缩放

#### 鼠标拖拽平移

通过 `mousedown` / `mousemove` / `mouseup` 事件在 `treeContainer` 上实现拖拽滚动。

#### 双指缩放（移动端）

通过 `touchstart` / `touchmove` 监听双指距离变化，动态修改 `.tree` 的 `transform: scale()`。

```javascript
state.scale = Math.min(Math.max(initialScale * zoomFactor, 0.3), 3);
elements.treeRoot.style.transform = `scale(${state.scale})`;
```

---

## 9. 核心业务流程

### 9.1 任务分解流程

```
用户输入任务描述 → [点击"开始分解"]
    │
    ├─ 检查：是否为空
    ├─ 提取文件内容（文本/图片 Base64）
    ├─ 关闭配置面板
    ├─ 显示加载页（随机一言）
    │
    ├─ fetchSSE(POST /api/decompose, { taskText, images })
    │       │
    │       ├─ 后端构建 Prompt
    │       ├─ 后端调用智谱 AI（SSE 流）
    │       └─ 透传流式数据到前端
    │
    ├─ 前端实时接收 & 拼接 JSON
    ├─ 完成 → 解析 JSON（去除 Markdown 标签）
    ├─ 渲染任务树
    ├─ [已登录] 自动保存到云端
    └─ 显示操作按钮（查看/修改、全局问答）
```

### 9.2 节点对话流程

```
用户点击节点"提问"按钮
    │
    ├─ 防并发检查（nodeAsking 锁）
    ├─ 弹出对话框，用户输入问题（可留空）
    │
    ├─ 创建空进度节点 → 渲染树
    ├─ fetchSSE(POST /api/ask, { nodeId, title, desc, userQuestion })
    │       │
    │       ├─ [后端] 查询 node_states 获取短记忆
    │       ├─ [后端] 构建上下文（summary + recent_chat）
    │       ├─ [后端] 调用 AI（SSE 流）
    │       ├─ [后端] 透传流到前端
    │       └─ [后端] 流结束后：
    │           ├─ 写入全量 chat_history
    │           ├─ 更新 node_states
    │           └─ 达 4 轮 → 触发 AI 摘要压缩
    │
    ├─ 前端实时更新进度预览
    ├─ [已登录] 自动保存树到云端
    └─ 释放并发锁
```

### 9.3 上下文压缩机制

```
对话轮次: 0 → 1 → 2 → 3 → [4] (触发压缩)
                                  │
                                  ├─ 构建摘要 Prompt:
                                  │   "已有总结: {summary}"
                                  │   "最新对话: {recent_chat}"
                                  │   → "请用中文回复，总结为不超过 200 字"
                                  │
                                  ├─ 调用 GLM-4.6V-FlashX（摘要模型）
                                  ├─ 新 summary 写入 node_states
                                  ├─ recent_chat 清空为 []
                                  └─ chat_count 重置为 0
```

### 9.4 登录与认证流程

#### 账号密码登录
```
输入用户名 + 密码 → POST /api/login
    ├─ 用户不存在 → 自动注册（bcrypt hash）→ 签发 JWT
    ├─ 用户存在 → 比对密码
    │   ├─ 匹配 → 签发 JWT
    │   └─ 不匹配 → 401 错误
    └─ 存储 JWT 到 localStorage + Cookie
```

#### WebAuthn 通行证登录
```
输入用户名 → POST /api/auth/login/options
    ├─ 查询用户已绑定的凭证
    └─ 生成 AuthenticationOptions

浏览器唤起生物识别 → POST /api/auth/login/verify
    ├─ 验证签名
    ├─ 更新 counter
    └─ 签发 JWT
```

### 9.5 密码找回流程

```
[Step 1] 输入用户名 → POST /api/password/send-reset-code
    ├─ 检查用户名 + 邮箱绑定
    ├─ 生成 6 位验证码（5 分钟有效）
    └─ 发送邮件（或开发模式返回 devCode）

[Step 2] 输入验证码 → (前端校验)

[Step 3] 输入新密码 → POST /api/password/reset
    ├─ 验证验证码有效性
    ├─ bcrypt hash 新密码
    └─ 更新数据库
```

---

## 10. 安全机制

### 10.1 认证安全

| 机制 | 说明 |
|------|------|
| JWT | HS256 签名，7 天有效期 |
| bcrypt | SALT_ROUNDS=10，密码哈希存储 |
| WebAuthn | 公钥密码学认证，counter 防重放 |
| CORS | 全局启用（生产环境建议限制 Origin） |
| 文件大小限制 | `express.json({ limit: '50mb' })` |

### 10.2 防越权

- 所有凭证操作（查询/删除/修改）均同时验证 `credential_id` 和 `user_id`
- 树数据操作均绑定 `user_id`

### 10.3 频率限制

- 密码重置验证码：60 秒内仅可发送一次
- 验证码有效期：5 分钟（到期自动清理）

### 10.4 已知安全注意点

- Challenge 暂存使用内存 Map（重启后丢失，可接受）
- 重置验证码使用内存 Map（同上，适合单实例部署）
- `EXPECTED_ORIGIN` 应与实际部署域名严格匹配
- 生产环境应修改 `JWT_SECRET` 为高强度随机字符串

---

## 11. 部署指南

### 11.1 本地开发

```bash
# 1. 克隆项目
git clone <repo-url>
cd ai-task-decomposer

# 2. 后端配置
cd backend
cp .env.example .env  # 编辑 .env 填入配置
npm install
npm start             # 默认监听 3000 端口

# 3. 前端
# 直接用浏览器打开 front/index.html
# 或使用任意静态服务器
npx serve front -p 8080
```

### 11.2 生产部署

```bash
# 后端
cd backend
npm install --production
npm start

# 前端：将 front/ 目录部署到 Nginx
# Nginx 配置示例：
# location / {
#     root /path/to/front;
#     try_files $uri $uri/ /index.html;
# }
# location /api/ {
#     proxy_pass http://127.0.0.1:3000;
#     proxy_set_header Host $host;
#     proxy_buffering off;  # SSE 必须
# }
```

### 11.3 Nginx SSE 配置要点

```nginx
location /api/ {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Connection '';
    proxy_buffering off;           # 关闭缓冲，SSE 实时推送
    proxy_cache off;
    proxy_read_timeout 300s;       # 防止长连接超时
}
```

### 11.4 进程管理（推荐 PM2）

```bash
npm install -g pm2
pm2 start backend/server.js --name ai-task-decomposer
pm2 save
pm2 startup   # 开机自启
```

---

## 附录 A：API 速查表

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| POST | `/api/login` | 无 | 登录/自动注册 |
| POST | `/api/user/email` | 严格 | 绑定邮箱 |
| POST | `/api/password/send-reset-code` | 无 | 发送重置码 |
| POST | `/api/password/reset` | 无 | 重置密码 |
| GET | `/api/auth/register/options` | 严格 | WebAuthn 注册选项 |
| POST | `/api/auth/register/verify` | 严格 | WebAuthn 注册验证 |
| POST | `/api/auth/login/options` | 无 | WebAuthn 登录选项 |
| POST | `/api/auth/login/verify` | 无 | WebAuthn 登录验证 |
| GET | `/api/auth/credentials` | 严格 | 获取通行证列表 |
| DELETE | `/api/auth/credentials/:id` | 严格 | 删除通行证 |
| PUT | `/api/auth/credentials/:id` | 严格 | 重命名通行证 |
| POST | `/api/decompose` | 无 | 任务分解（SSE） |
| POST | `/api/import` | 无 | 导入导图 |
| POST | `/api/ask` | 宽松 | 节点对话（SSE） |
| GET | `/api/history` | 严格 | 节点聊天历史 |
| GET | `/api/trees` | 严格 | 历史树列表 |
| GET | `/api/trees/:id` | 严格 | 树详情 |
| POST | `/api/trees` | 严格 | 保存新树 |
| PUT | `/api/trees/:id` | 严格 | 更新树 |
| POST | `/api/chat-tree` | 无 | 全局问答（SSE） |
| GET | `/api/chat-tree/history/:treeId` | 无 | 全局对话历史 |

## 附录 B：前端 CDN 依赖

| CDN 地址 | 用途 |
|---------|------|
| `fonts.googleapis.com/css2?family=Roboto:wght@400;500;600;700` | Roboto 字体 |
| `fonts.googleapis.com/icon?family=Material+Icons+Round` | Material Icons |
| `cdn.jsdelivr.net/npm/cn-fontsource-source-han-serif-sc-vf-regular/font.css` | 思源宋体 |
| `cdn.jsdelivr.net/npm/marked/marked.min.js` | Markdown 渲染 |
| `cdn.jsdelivr.net/npm/html2canvas@1.4.1/dist/html2canvas.min.js` | 图片导出 |
| `cdn.jsdelivr.net/npm/@simplewebauthn/browser/dist/bundle/index.umd.min.js` | WebAuthn |

---

*文档完*
