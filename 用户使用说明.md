# AI 任务分解与执行系统 用户使用说明

欢迎使用 **AI 任务分解与执行系统**！本系统是一款基于大语言模型的项目规划与执行助手，旨在通过层级化的思维导图界面，帮助您将复杂的项目目标拆解为可执行的细小步骤，并提供实时的 AI 指导。

---

## 1. 核心概念与界面导航

### 全景画布
- **平移 (Pan)**：在空白区域长按/按住并拖动鼠标（桌面端）或手指（移动端）可自由移动画布。
- **缩放 (Zoom)**：使用鼠标滚轮（桌面端）或双指捏合（移动端）可放大/缩小任务树，查看宏观结构或微观细节。

### 修改模式 (Edit Mode)
- **管理模式**：用于手动调整任务树的结构，包括重排序、新增或删除步骤。在此模式下，主任务设定按钮将被隐藏以提供更广阔的编辑空间。
- **撤销与重做**：
    - 在修改模式底部中央，您可以找到“撤销”和“重做”按钮。
    - **快捷键 (仅桌面端)**：支持 `Ctrl + Z` 进行撤销，`Ctrl + Y` 或 `Ctrl + Shift + Z` 进行重做。
    - **撤销范围**：支持撤销节点的新增、删除、位置移动以及单一节点的提问内容。

---

## 2. 任务树的创建与管理

### 任务分解
1. 在主输入框中输入您的项目目标（例如：“策划一场 50 人的户外婚礼”）。
2. 点击“开始分解”，AI 将自动生成多周/多阶段的任务树。

### 手动编辑 (修改模式下)
- **拖拽重排序**：按住节点气泡（桌面端）或通过特定手势（移动端）将其拖动到另一个节点上，可改变其所属关系。*系统会自动防止循环移动。*
- **删除节点**：点击节点右上角的 `X` 图标可递归删除该步骤及其所有下游任务。
- **新增子步骤**：点击节点底部的 `+` 图标，在弹出的 MD3 对话框中输入标题和详细描述，即可手动扩展您的任务流。

---

## 3. 任务执行与 AI 助手

### 单节点提问 (查看模式下)
点击任何节点上的“提问”或“向 AI 索要执行方案”按钮：
- **方案获取**：留空并确认，AI 将根据该步骤的上下文提供详细的操作指南。
- **具体疑问**：输入具体问题（如：“这个步骤需要用到哪些特定工具？”），AI 将针对性回答。
- **进度查看**：生成后的内容将以卡片形式挂载在节点下方，点击带有倒三角符号 (▼) 的进度卡片可查看完整内容。

### 项目全景助手
在页面右下角（移动端为悬浮预览按钮），您可以打开“项目全景助手”：
- 该助手感知整棵任务树。您可以提问：“这个项目的关键路径是什么？”或“哪些步骤存在潜在风险？”
- 所有的对话历史将自动保存至云端。
- **移动端提示**：如果调出助手后顶栏被浏览器遮挡，请尝试开启浏览器的全屏模式。

---

## 4. 账户、同步与安全

### 登录与记录恢复
- **个人中心**：点击顶栏头像图标可进行登录。
- **云端恢复**：在个人中心可查看历史生成的任务树，点击即可恢复到当前画布进行编辑。

### 免密安全 (Passkeys)
- 在个人中心，您可以绑定“设备通行证 (Passkey)”。
- 绑定后，在支持的设备上您无需输入密码，即可通过指纹、面容或设备锁屏密码快速安全地登录。

---

## 5. 提示说明
- **Markdown 支持**：系统内所有 AI 生成的内容和对话均支持 Markdown 渲染（加粗、列表、代码块等）。
- **实时保存**：在登录状态下，您对任务树的任何修改（如新增节点、调整位置、生成进度）都会实时同步至服务器。

> [!IMPORTANT]
> **警告**：直接清空画布或删除节点是不可逆操作，系统会通过内置的 MD3 对话框进行二次确认。请在操作前仔细检查。
