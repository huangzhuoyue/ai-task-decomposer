# AI 任务分解与执行系统 — 用户使用说明书

欢迎使用 **AI 任务分解与执行系统**！本系统是一款基于大语言模型（智谱 GLM-4.6V）的项目规划与执行助手，通过层级化的思维导图界面，帮助您将复杂的项目目标拆解为可执行的细小步骤，并提供实时 AI 指导。

---

## 目录

1. [项目概述](#1-项目概述)
2. [访问方式](#2-访问方式)
3. [账户系统](#3-账户系统)
4. [界面导航](#4-界面导航)
5. [任务分解与导入](#5-任务分解与导入)
6. [任务树编辑](#6-任务树编辑)
7. [AI 对话功能](#7-ai-对话功能)
8. [数据导出与管理](#8-数据导出与管理)
9. [个人中心](#9-个人中心)
10. [开发者模式](#10-开发者模式)
11. [快捷键](#11-快捷键)
12. [常见问题](#12-常见问题)

---

## 1. 项目概述

本系统提供以下核心能力：

| 功能 | 说明 |
|------|------|
| **AI 任务分解** | 输入项目描述，AI 自动生成层级化的任务树 |
| **导入已有导图** | 上传思维导图图片或 Markdown 文件，AI 自动解析为结构化任务树 |
| **单节点提问** | 对任意任务步骤与 AI 对话，获取执行方案或针对性建议 |
| **项目全景助手** | AI 感知整棵任务树，提供全局性分析与建议 |
| **云端同步** | 登录后任务树自动保存至云端，支持跨设备恢复 |
| **免密登录** | 支持 WebAuthn 通行证（指纹/面容/设备密码） |
| **主题定制** | 5 种主题色可选 |
| **图片导出** | 一键将任务树导出为高清 PNG 图片 |

---

## 2. 访问方式

系统提供桌面端和移动端两个版本：

| 版本 | 地址 | 说明 |
|------|------|------|
| 桌面端 | `https://hzyweb.xyz` | 适配电脑浏览器，支持键盘快捷键 |
| 移动端 | `https://m.hzyweb.xyz` | 适配手机/平板，触摸手势优化 |

> 💡 **移动端建议**：在浏览器菜单中选择"添加到主屏幕"，以全屏模式获得最佳体验。

---

## 3. 账户系统

### 3.1 登录与自动注册

1. 点击顶栏右上角的 **登录图标** 进入登录页
2. 输入**用户名**和**密码**
3. 点击 **登录 / 注册**
   - 如果账号不存在，系统将**自动注册**并登录
   - 如果账号已存在，将验证密码后登录

### 3.2 WebAuthn 通行证（免密登录）

通行证是一种基于 FIDO2 标准的安全认证方式，绑定后可免输入密码快速登录。

**绑定通行证**：
- 首次登录成功后，系统会弹窗询问是否绑定当前设备
- 或在**个人中心**中点击"绑定当前设备"
- 绑定时需为设备起名（如"我的手机"），并完成生物识别验证（指纹/面容/PIN）

**使用通行证登录**：
1. 在登录页输入**用户名**
2. 点击 **使用生物识别 / 通行证登录**
3. 完成设备生物识别即可

### 3.3 访客模式

如果不登录，仍可使用任务分解和单节点提问功能，但**无法**：
- 云端保存/恢复任务树
- 使用项目全景助手
- 绑定通行证

### 3.4 密码找回（仅桌面端）

> ⚠️ 使用此功能前，需先在个人中心绑定邮箱。

1. 在登录页点击 **忘记密码？**
2. **第一步**：输入用户名 → 点击"发送验证码"
3. **第二步**：输入收到的 6 位验证码 + 设置新密码 → 点击"确认重置密码"
4. **第三步**：重置成功，使用新密码登录

验证码 **5 分钟**内有效，发送间隔限制为 **60 秒**。

---

## 4. 界面导航

### 4.1 顶栏

顶栏包含以下功能按钮（从左到右）：

| 图标 | 功能 |
|------|------|
| 🌳 **Logo** | 应用标识 |
| 👁 / ✏️ **模式切换** | 查看/编辑模式切换（任务树存在时显示） |
| 👤 **登录/个人中心** | 未登录时跳转登录页，已登录时打开个人中心 |
| 🖼 **导出** | 将任务树导出为 PNG 图片 |
| 🗑 **清空** | 清空当前画布所有节点 |

### 4.2 全景画布

- **平移**：在空白区域按住并拖动鼠标（桌面端）或单指滑动（移动端）
- **缩放**：鼠标滚轮（桌面端）或双指捏合（移动端），缩放范围 0.3x ~ 2x

### 4.3 左侧配置面板（桌面端）/ 底部面板（移动端）

点击右下角 **"+"按钮** 打开，包含：
- 工作模式选择（任务分解 / 导入已有导图）
- 任务描述输入框
- 附件上传（支持图片和文本文件）
- 主题颜色选择器
- 云端历史记录列表（登录后显示）

---

## 5. 任务分解与导入

### 5.1 AI 任务分解

1. 点击右下角 **"+"按钮** 打开配置面板
2. 选择 **任务分解** 模式
3. 在任务描述框中输入项目目标，例如：
   - "策划一场 50 人的户外婚礼"
   - "从零开始开发一个电商小程序"
   - "准备一次为期两周的日本旅行"
4. （可选）上传附件（图片或 `.txt`/`.md` 文件作为参考材料）
5. 点击 **开始分解**
6. 等待 AI 生成（期间会显示加载动画和随机名言）
7. 生成完成后，任务树将自动渲染到画布上

### 5.2 导入已有导图

如果您已有思维导图，可以让 AI 自动识别并转为结构化任务树：

1. 打开配置面板，选择 **导入已有导图** 模式
2. 上传思维导图的 **图片**（PNG/JPEG/WebP）或包含层级结构的 **文本文件**（.txt/.md）
3. 点击 **开始分解**
4. AI 将识别图中的节点和层级关系，生成结构化任务树

### 5.3 附件说明

| 文件类型 | 处理方式 |
|----------|----------|
| 图片 (PNG/JPEG/WebP) | 由 GLM-4.6V 多模态模型直接识别 |
| 文本 (.txt/.md) | 提取内容作为文本参考附加到任务描述中 |

---

## 6. 任务树编辑

### 6.1 查看 vs 编辑模式

| 模式 | 可用操作 |
|------|----------|
| **查看模式** (👁) | 单节点提问、查看进度、打开全景助手、缩放/平移 |
| **编辑模式** (✏️) | 拖拽排序、删除节点、新增子节点、撤销/重做 |

通过顶栏的**模式切换**按钮或分段控件切换。

### 6.2 拖拽重排序

在编辑模式下，拖拽节点到另一个节点上，即可改变其层级关系：
- 系统会自动**防止循环移动**（不能把父节点拖入自己的子节点中）
- 节点边框会变为虚线以提示编辑状态

### 6.3 删除节点

在编辑模式下，点击节点右上角的 **X** 按钮：
- 系统会弹出确认对话框
- 确认后将**递归删除**该节点及其所有子节点和关联的进度数据
- 此操作**可撤销**

### 6.4 新增子步骤

在编辑模式下，点击节点底部的 **+** 按钮：
- 在弹出的对话框中输入**标题**和**描述**
- 确认后将在该节点下新增一个子步骤

### 6.5 撤销与重做

系统支持最多 **30 步**撤销历史，可撤销操作包括：
- 节点的新增、删除
- 节点的拖拽移动
- 单节点的提问内容

**操作方式**：
- **桌面端**：编辑模式下点击底部浮动的撤销/重做按钮，或使用快捷键 `Ctrl+Z` / `Ctrl+Y`
- **移动端**：编辑模式下底部自动出现撤销/重做浮动工具栏

---

## 7. AI 对话功能

### 7.1 单节点提问

在**查看模式**下，点击任何节点上的 **提问** 按钮：

1. 在弹出的对话框中输入问题（或留空）
2. **留空** → AI 将根据该步骤的上下文生成详细的执行方案
3. **输入具体问题** → AI 将针对问题作答，例如："这个步骤需要哪些工具？"
4. AI 回复将以流式输出实时显示
5. 回复完成后，内容以**进度卡片**形式挂载在节点下方

**进度卡片查看**：
- 点击带有 **▼** 符号的进度卡片，在右侧（桌面端）或底部面板（移动端）查看完整 Markdown 渲染内容

**上下文记忆**：
- 系统会自动记忆每个节点的对话历史
- 对话达到 4 轮后，会自动压缩为摘要，避免上下文过长
- 登录用户的全量历史会持久化到云端

### 7.2 项目全景助手

全景助手能够感知**整棵任务树**的结构和进度，回答全局性问题。

**打开方式**：
- **桌面端**：点击页面右下角的 **💬 对话图标**
- **移动端**：点击底部浮动的 **💬 对话图标**（编辑模式下位置可能变化）

**使用方式**：
1. 在对话框中输入问题，例如：
   - "这个项目的关键路径是什么？"
   - "哪些步骤可能存在风险？"
   - "帮我估算一下总工期"
   - "有没有可以并行执行的任务？"
2. AI 将结合整棵树的结构和已有进度进行分析回答
3. 对话历史自动保存，再次打开时会恢复之前的对话

> 💡 提示：移动端如果顶栏被浏览器遮挡，请尝试使用浏览器的全屏模式。

---

## 8. 数据导出与管理

### 8.1 导出为图片

点击顶栏的 **导出按钮**，系统会将当前任务树以 2 倍分辨率导出为 PNG 图片并自动下载。

### 8.2 云端历史记录（需登录）

在配置面板中可以查看历史记录列表：
- 显示任务标题和创建时间
- 点击任意历史记录可**恢复**到当前画布继续编辑

### 8.3 实时同步

登录状态下，以下操作会自动同步到云端：
- 任务分解完成
- 导入导图完成
- 新增/删除/移动节点
- 向节点提问并生成进度

### 8.4 清空画布

点击顶栏的 **清空按钮**：
- 系统会弹出二次确认对话框
- 确认后清除当前画布的所有节点和进度
- 此操作**不可撤销**

---

## 9. 个人中心

点击顶栏的 **个人中心图标**（已登录状态下）打开，包含以下功能：

### 9.1 通行证管理

- 查看已绑定的所有通行证设备列表（设备名称 + 绑定时间）
- **重命名**设备：点击编辑图标修改设备名称
- **解绑**设备：点击删除图标移除通行证
- **绑定新设备**：点击"绑定当前设备"按钮

### 9.2 绑定邮箱

绑定邮箱后可通过邮件验证码找回密码：

1. 在邮箱输入框中输入邮箱地址
2. 点击"保存"
3. 绑定成功后会显示确认提示

> ⚠️ 每个邮箱只能绑定一个账号。如提示"已被其他账户绑定"，请更换邮箱。

### 9.3 退出登录

点击"退出登录"按钮，确认后：
- 清除本地登录凭证
- 跳回登录状态
- 本地数据不受影响

---

## 10. 开发者模式

开发者模式允许您使用自定义的 AI API 接口替代默认的智谱 GLM-4.6V。

### 10.1 启用方式

1. 打开**个人中心**
2. 找到"开发者模式 (自定义 API)"区域
3. 勾选"启用开发者自定义 API"

### 10.2 配置项

| 字段 | 说明 |
|------|------|
| **API Key** | 您的 AI 服务 API 密钥（必填） |
| **Base URL** | API 地址（默认 `https://api.openai.com/v1`） |
| **Model Name** | 模型名称（默认 `glm-4.6v`） |
| **Custom Body JSON** | 自定义请求体参数，如 `{"temperature": 0.7}` |

### 10.3 注意事项

- 配置保存在浏览器本地（localStorage），不会上传到服务器
- API 需兼容 OpenAI 格式（`/chat/completions` 端点）
- 自定义配置通过 HTTP Header 透传到后端

---

## 11. 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + Z` | 撤销上一步操作 |
| `Ctrl + Y` / `Ctrl + Shift + Z` | 重做 |
| `Enter`（对话框中） | 确认对话框 |
| 鼠标滚轮 | 缩放画布 |
| 鼠标拖拽空白区域 | 平移画布 |

---

## 12. 常见问题

### Q: 任务分解失败怎么办？
- 检查网络连接是否正常
- 尝试简化任务描述，使用更明确的语言
- 如果使用开发者模式，检查 API Key 和 Base URL 是否正确

### Q: 为什么无法使用全景助手？
- 需要先完成一次任务分解，生成任务树后才能使用
- 确保当前处于查看模式（非编辑模式）

### Q: 移动端浏览器遮挡了功能栏怎么办？
- 将应用"添加到主屏幕"以全屏模式运行
- 或尝试使用浏览器的全屏/隐藏工具栏功能

### Q: 通行证登录失败？
- 确保该用户名下已绑定通行证
- 确保使用的是支持 WebAuthn 的浏览器（Chrome、Edge、Safari 等现代浏览器）
- 确保页面通过 HTTPS 访问（WebAuthn 要求安全上下文）

### Q: 密码找回提示"未绑定邮箱"？
- 需先登录账号，在个人中心绑定邮箱
- 邮箱绑定后才能使用密码找回功能

### Q: 数据会丢失吗？
- 登录用户的数据会自动同步到云端，即使更换设备也可恢复
- 未登录用户的数据仅保存在浏览器本地，清除浏览器数据会丢失

---

## 技术信息

| 项目 | 说明 |
|------|------|
| **AI 模型** | 智谱 GLM-4.6V（多模态，支持图片理解） |
| **输出方式** | SSE 流式输出，实时显示生成过程 |
| **数据存储** | SQLite（后端） |
| **认证方式** | JWT（7天有效期） + WebAuthn (FIDO2) |
| **前端技术** | 原生 HTML/CSS/JS，Material Design 3 风格 |
| **后端技术** | Node.js + Express 5 |

---

> 📖 本说明书最后更新于 2026 年 4 月。如有功能更新，请以实际系统界面为准。
